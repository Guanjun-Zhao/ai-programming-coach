# AI 编程教练 · 小组分工与时间线
## 1. 总原则

- 小组成员：**赵冠钧（组长）**、**李佳昱**、**师圣晴**。

- **当前进度**：截至本文更新，应用程序**已实现的功能**（含主页 API 校验与配置、版本页三列、任务树、AI 教练与验证、Debug 样例流程、`data/` 下持久化等）均由 **组长赵冠钧** 完成开发与落地。

- **代码量（仅运行时程序，与《程序实现_详细说明》所列模块一致）**：共 **1759** 行有效 Python（7 个文件：`main.py` 20，`main_window.py` 284，`version_page.py` 202，`chat_widget.py` 700，`ai_coach.py` 232，`data_manager.py` 184，`sections_loader.py` 137）。**不含** `scripts/` 下章节/样例分割脚本、`answer/` 下参考答案 C++、以及 `prompts/*.txt`。
**有效行**口径：去掉纯空行与「整行仅为 `#` 注释」后计行；同一行内代码与 `#` 尾部注释计 1 行。

- **后续分工**：
  - **李佳昱**：负责 **版本三、版本四** 的**完整实现**（与两版本对应的章节数据、样例、以及 `data/version3/`、`data/version4/` 等资源与行为闭环；在既有架构上对齐 `sections.json` / `samples.json` 等约定）。
  - **师圣晴**：负责 **前端交互优化与界面美化**（在现有 `main_window.py`、`version_page.py`、`chat_widget.py` 等 PyQt6 代码上迭代布局、样式与易用性）。
- 赵冠钧继续承担 **架构与接口协调**、**代码审查**及关键模块维护，保证组员改动与既有设计一致。

---

## 2. 代码模块分工

| 成员 | 职责定位 |
|------|----------|
| **赵冠钧（组长）** | 已完成当前主流程与核心模块实现（如 `ai_coach.py`、`data_manager.py`、`sections_loader.py`、`prompts/`、主页与版本页骨架等）；后续以审查、接口协调与难点支持为主。 |
| **李佳昱** | **后续**：版本三、版本四的完整实现（数据内容、样例与版本目录下资源的齐备与可运行闭环；`data_manager` 等既有接口的对接与扩展）。 |
| **师圣晴** | **后续**：界面与交互优化、视觉美化（任务树、对话区、代码编辑区等组件的体验与一致性）。 |

**衔接说明**：版本三、四的功能与数据以组长已实现的主干为基准；李佳昱在 **`task_id`、历史路径、`sections.json` / `samples.json` 形态** 上与组长约定对齐；师圣晴的 UI 改动避免破坏业务逻辑与信号槽契约。

---

## 3. 内容准备分工（与代码开发并行）

| 成员 | 内容任务 |
|------|----------|
| 赵冠钧 | 主导「真理链」与全局 `sections.json` 规则；为版本三、四提供切割规范与评审。 |
| 李佳昱 | **版本三、版本四**：教学文档细化分割、各节 `description`/`code` 与 `samples.json` 的落实及与题目一致性的核对。 |
| 师圣晴 | 与界面相关的任务清单展示、文案与交互提示的优化（与题目章节对照）。 |

---

## 4. 题目理解如何分工（重要前提）

- **目标**：写入产品的 System Prompt、样例标签、任务文案建立在 **可验收的规则** 之上；不追求组员人人都会手写魔兽世界 AC。
- **真理链（专人）**：赵冠钧负责规则正文与跨版本易混点，对照仓库 `problems/*.md`。
- **浅层对照（协作者）**：李佳昱在版本三、四内容与样例上与赵冠钧对齐；师圣晴在界面与任务展示上与题目功能划分对照，不确定处由赵冠钧答疑。

---

## 5. 可用工时

| 成员 | 周工时 |
|------|--------|
| 赵冠钧 | 约 14 小时（每天 2 小时） |
| 李佳昱 | 约 5 小时 |
| 师圣晴 | 约 5 小时 |

---

## 6. 阶段规划与时间线

**说明**：以下各阶段 **起止日期与里程碑不变**；进入对应阶段工作时，采用 **§1「后续分工」** 中的职责划分（当前主干已由赵冠钧实现，组员工作侧重版本三、四与界面优化）。

### 6.1 准备阶段：5 月 2 日 — 5 月 4 日（文档完善与团队对齐）

- **赵冠钧**：完善功能设计文档，确认所有功能决策无歧义。
- **三人**：通读文档，对齐项目方向、分工和时间节点。
- **5 月 4 日**：提交功能设计文档给助教。

---

### 6.2 第一阶段：5 月 4 日 — 5 月 11 日（等助教反馈，1 周）

> 策略：不大量写业务代码，先跑通界面与接口链路；System Prompt 与 `sections.json` 允许占位迭代。

- **赵冠钧**：搭 GitHub 仓库结构，写模块接口文档，生成 PyQt 基础模板，切割第一版 `sections.json`。
- **李佳昱**：整理标注老师样例，产出第一版 `samples.json`（与后续版本三、四扩展衔接）。
- **师圣晴**：整理任务清单文案，熟悉 PyQt 模板。

---

### 6.3 第二阶段：5 月 11 日 — 5 月 29 日（核心开发，3 周）

> 根据助教反馈微调后全力开发；**本阶段起**执行 §1 后续分工：李佳昱主攻版本三、四完整实现，师圣晴主攻界面优化与美化，赵冠钧审查与协调。

- **赵冠钧**：代码审查、关键接口与 `ai_coach` / 数据结构协调；必要时补位难点。**本阶段 Python 侧重**：`ai_coach.py`、`data_manager.py`、`sections_loader.py`；代码审查可覆盖仓库内全部 `.py`。
- **李佳昱**：**版本三、版本四** 完整实现（`sections.json`、`samples.json` 及对应 `data/version3/`、`data/version4/` 下资源闭环）。**本阶段 Python 侧重**：`scripts/build_sections.py`、`scripts/build_samples.py`；若章节/样例与 UI、AI 的 JSON 契约变化，按需改 `sections_loader.py`、`data_manager.py`。
- **师圣晴**：**前端优化与界面美化**（主页、版本页、对话与代码区等）。**本阶段 Python 侧重**：`main_window.py`、`version_page.py`、`chat_widget.py`（入口 `main.py` 一般无需为美化修改）。

非 Python 的 JSON 与其它资源仍由各角色在 `data/`、`prompts/` 等目录维护；上列为与职责最相关的 Python 文件。

---

### 6.4 第三阶段：5 月 29 日 — 6 月 6 日（集成联调，1 周）

- **三人协作**：合并版本三、四与界面改动，跑通完整用户流程。
- 录制初版演示视频（4 分钟以内，mp4 格式）。
- **6 月 6 日**：提交初版录屏。

---

### 6.5 第四阶段：6 月 6 日 — 7 月 6 日（打磨，4 周）

- 根据助教反馈修 bug。
- 优化 System Prompt 与 `sections.json` 质量（对 AI 教练体验影响最大）。
- 持续完善界面细节与版本三、四内容质量。
- 撰写作业报告（10 页以内，含功能介绍、模块设计、分工情况、总结反思）。
- **7 月 6 日**：最终提交 GitHub 链接。

---

## 7. 里程碑一览

| 日期 | 事项 |
|------|------|
| 5 月 4 日 | 提交功能设计文档给助教 |
| 5 月 15 日 | 中期检查（每人展示当前进度） |
| 6 月 6 日 | 提交初版录屏 |
| 7 月 6 日 | 最终提交 GitHub 链接 |

---

## 8. 与分工相关的风险摘要

（摘自原方案风险表，仅保留与人力、协作、接口相关的条目；技术比对细节见 `程序实现_详细说明.md`。）

| 风险 | 概率 | 应对 |
|------|------|------|
| 师圣晴 PyQt 学习曲线导致界面延误 | 高 | 赵冠钧已提供可运行主干；后续以在现有代码上迭代为主 |
| 集成阶段模块接口不兼容 | 中 | 版本三、四与 UI 改动严格对齐既有 `task_id` 与数据路径约定；赵冠钧审查 |
| `sections.json` 切割工作量大 | 中 | 李佳昱分版本推进；可用 AI 辅助提取 description |
| 队友工时不足（课程冲突等） | 中 | 5 月 15 日中期检查提前发现；赵冠钧可用 Cursor 补位 |
| System Prompt 验证精度不稳定 | 中 | 第四阶段留专门时间迭代；初版允许偶发误判 |
| AI 生成样例质量不稳定 | 低 | 开发阶段人工核查所有 AI 生成样例再写入 |

---

*本文档用于团队内部对齐；与《AI编程教练_项目方案总结.md》如有出入，以本文「当前进度 / 后续分工」与仓库实现为准。*
